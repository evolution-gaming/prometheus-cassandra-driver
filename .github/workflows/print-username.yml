name: Print Secrets

on:
  workflow_dispatch:

jobs:
  print-secrets:
    runs-on: ubuntu-latest
    
    steps:
      - name: Print Sonatype Username
        run: |
          USERNAME="${{ secrets.SONATYPE_USERNAME }}"
          echo "SONATYPE_USERNAME=${USERNAME:0:12}"
